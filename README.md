# cardintel

`cardintel` is organized as a single repository with independently deployable frontend and backend applications.

## Structure

```text
cardintel/
├── frontend/    # Client application
└── backend/     # Server application
```

Each application owns its dependencies, configuration, and documentation.
